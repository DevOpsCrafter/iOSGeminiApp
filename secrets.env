# Local secrets file (DO NOT commit real values to the repository)
# Rename secrets.env.template -> secrets.env and fill in your own values locally.
# Keep this file out of version control and add it to .gitignore (already added).
GEMINI_API_KEY=YOUR_GEMINI_API_KEY_HERE
YOUR_EMAIL=you@example.com
EMAIL_PASSWORD=your_app_password_here
